# Copy to .env (or set in your process manager) before the first run.
PORT=3400
HOST=127.0.0.1
SITE_URL=https://promptaria.aliizz.ir

# Seeds the admin password on FIRST boot only. Afterwards the hash lives in the
# database and is changed from the panel's Settings tab.
ADMIN_PASSWORD=change-me
